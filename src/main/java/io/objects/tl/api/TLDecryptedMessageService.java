package io.objects.tl.api;

import static io.objects.tl.StreamUtils.*;
import static io.objects.tl.TLObjectUtils.*;

import io.objects.tl.TLContext;
import java.io.IOException;
import java.io.InputStream;
import java.io.OutputStream;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;

/**
 * This class is generated by Mono's TL class generator
 */
public class TLDecryptedMessageService extends TLAbsDecryptedMessage {
    public static final int CONSTRUCTOR_ID = 0x73164160;

    protected TLAbsDecryptedMessageAction action;

    private final String _constructor = "decryptedMessageService#73164160";

    public TLDecryptedMessageService() {
    }

    public TLDecryptedMessageService(long randomId, TLAbsDecryptedMessageAction action) {
        this.randomId = randomId;
        this.action = action;
    }

    @Override
    public void serializeBody(OutputStream stream) throws IOException {
        writeLong(randomId, stream);
        writeTLObject(action, stream);
    }

    @Override
    @SuppressWarnings({"unchecked", "SimplifiableConditionalExpression"})
    public void deserializeBody(InputStream stream, TLContext context) throws IOException {
        randomId = readLong(stream);
        action = readTLObject(stream, context, TLAbsDecryptedMessageAction.class, -1);
    }

    @Override
    public int computeSerializedSize() {
        int size = SIZE_CONSTRUCTOR_ID;
        size += SIZE_INT64;
        size += action.computeSerializedSize();
        return size;
    }

    @Override
    public String toString() {
        return _constructor;
    }

    @Override
    public int getConstructorId() {
        return CONSTRUCTOR_ID;
    }

    public long getRandomId() {
        return randomId;
    }

    public void setRandomId(long randomId) {
        this.randomId = randomId;
    }

    public TLAbsDecryptedMessageAction getAction() {
        return action;
    }

    public void setAction(TLAbsDecryptedMessageAction action) {
        this.action = action;
    }
}
