package io.objects.tl.api;

import io.objects.tl.TLContext;
import io.objects.tl.api.help.TLChannelMigrationInfo;
import io.objects.tl.core.TLMethod;
import io.objects.tl.core.TLObject;

import java.io.IOException;
import java.io.InputStream;

import static io.objects.tl.StreamUtils.readTLObject;

/**
 * This class is generated by Mono's TL class generator
 */
public class TLRequestHelpGetChannelMigrationInfo extends TLMethod<TLChannelMigrationInfo> {
    public static final int CONSTRUCTOR_ID = 0x42BBD6FF;

    private final String _constructor = "help.getChannelMigrationInfo#42BBD6FF";

    public TLRequestHelpGetChannelMigrationInfo() {
    }

    @Override
    @SuppressWarnings({"unchecked", "SimplifiableConditionalExpression"})
    public TLChannelMigrationInfo deserializeResponse(InputStream stream, TLContext context) throws IOException {
        final TLObject response = readTLObject(stream, context);
        if (response == null) {
            throw new IOException("Unable to parse response");
        }
        if (!(response instanceof TLChannelMigrationInfo)) {
            throw new IOException("Incorrect response type, expected " + getClass().getCanonicalName() + ", found " + response.getClass().getCanonicalName());
        }
        return (TLChannelMigrationInfo) response;
    }

    @Override
    public String toString() {
        return _constructor;
    }

    @Override
    public int getConstructorId() {
        return CONSTRUCTOR_ID;
    }
}