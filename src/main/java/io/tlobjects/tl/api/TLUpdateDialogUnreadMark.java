package io.tlobjects.tl.api;

import static io.tlobjects.tl.StreamUtils.*;
import static io.tlobjects.tl.TLObjectUtils.*;

import io.tlobjects.tl.TLContext;
import java.io.IOException;
import java.io.InputStream;
import java.io.OutputStream;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;

/**
 * This class is generated by Mono's TL class generator
 */
public class TLUpdateDialogUnreadMark extends TLAbsUpdate {
    public static final int CONSTRUCTOR_ID = 0xe16459c3;

    protected int flags;

    protected boolean unread;

    protected TLDialogPeer peer;

    private final String _constructor = "updateDialogUnreadMark#e16459c3";

    public TLUpdateDialogUnreadMark() {
    }

    public TLUpdateDialogUnreadMark(boolean unread, TLDialogPeer peer) {
        this.unread = unread;
        this.peer = peer;
    }

    private void computeFlags() {
        flags = 0;
        flags = unread ? (flags | 1) : (flags & ~1);
    }

    @Override
    public void serializeBody(OutputStream stream) throws IOException {
        computeFlags();

        writeInt(flags, stream);
        writeTLObject(peer, stream);
    }

    @Override
    @SuppressWarnings({"unchecked", "SimplifiableConditionalExpression"})
    public void deserializeBody(InputStream stream, TLContext context) throws IOException {
        flags = readInt(stream);
        unread = (flags & 1) != 0;
        peer = readTLObject(stream, context, TLDialogPeer.class, TLDialogPeer.CONSTRUCTOR_ID);
    }

    @Override
    public int computeSerializedSize() {
        computeFlags();

        int size = SIZE_CONSTRUCTOR_ID;
        size += SIZE_INT32;
        size += peer.computeSerializedSize();
        return size;
    }

    @Override
    public String toString() {
        return _constructor;
    }

    @Override
    public int getConstructorId() {
        return CONSTRUCTOR_ID;
    }

    public boolean getUnread() {
        return unread;
    }

    public void setUnread(boolean unread) {
        this.unread = unread;
    }

    public TLDialogPeer getPeer() {
        return peer;
    }

    public void setPeer(TLDialogPeer peer) {
        this.peer = peer;
    }
}
